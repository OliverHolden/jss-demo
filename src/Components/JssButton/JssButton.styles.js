import {createUseStyles} from "react-jss";

export default createUseStyles({

    button: {
        backgroundColor: '#2778b3',
        width: 200,
        border: [5, 'solid', '#fff'],
        fontSize: 30,
        fontFamily: 'Calibri',
        color: '#fff',
    },
});