import {createUseStyles} from "react-jss";

export default createUseStyles({
    button: {
        width: 200,
        height: 100,
        backgroundColor: '#FF0000',
        border: [5, 'solid', '#fff'],
        borderRadius: '25px',
        fontFamily: "Calibri",
        fontSize: '1.8em',
        color: '#fff',
    },
});
