import React from "react";
import './NewStyledButton.scss'

export default function NewStyledButton() {
    return (
        <button className={"button button-blue"}>New Styled Button</button>
    );
}
